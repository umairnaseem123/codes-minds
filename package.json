{
  "name": "codes-minds-fullstack",
  "private": true,
  "scripts": {
    "dev": "npm --prefix codes-minds run dev",
    "build": "npm install --prefix codes-minds && npm run build --prefix codes-minds",
    "lint": "npm run lint --prefix codes-minds",
    "vercel-build": "npm install --prefix codes-minds && npm run build --prefix codes-minds"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.2",
    "multer": "^2.2.0",
    "multer-storage-cloudinary": "^4.0.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "oxlint": "^1.78.0",
    "vite": "^8.2.1"
  }
}
